version: '3'

services:
  postgres:
    image: postgres:15
    container_name: qris-postgres
    environment:
      POSTGRES_USER: qris
      POSTGRES_PASSWORD: qris
      POSTGRES_DB: qris_db
    ports:
      - "5432:5432"